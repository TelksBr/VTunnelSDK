{
  "name": "__PROJECT_NAME__",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:android": "npm run build && node ./scripts/build-android-html.mjs --dist dist --out dist/build.html",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "vtunnel-sdk": "^__VTUNNEL_SDK_VERSION__"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.2.0"
  }
}
